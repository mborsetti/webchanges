# Jobs for test cases for hooks.rst used by test_docs_hooks.py
# Each heading is a unique pseudo-url
# Ensure you use a Unicode character (e.g. ™ / &trade) in your example (both input and output) to test that too!

# yaml-language-server: $schema=jobs.schema.json
url: https://example.org/case
filters:
  - hooks_case:
---
# yaml-language-server: $schema=jobs.schema.json
url: https://example.org/indent
filters:
  - hooks_indent:
---
# yaml-language-server: $schema=jobs.schema.json
url: https://example.org/
---
# yaml-language-server: $schema=jobs.schema.json
url: https://example.org/.foobar
